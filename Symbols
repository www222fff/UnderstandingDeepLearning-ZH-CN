在数学和统计学中，模型参数通常使用希腊字母表示，常见的符号及其读法包括：

α（Alpha）：

读作“阿尔法”，常用来表示截距或某些常数参数。
β（Beta）：

读作“贝塔”，常用来表示回归模型中的斜率参数。
γ（Gamma）：

读作“伽马”，常用来表示某些分布的参数或时序模型中的参数。
δ（Delta）：

读作“德尔塔”，常用来表示差异或变化量。
θ（Theta）：

读作“西塔”或“塞塔”，常用来表示参数的一般符号，尤其是在最大似然估计中。
λ（Lambda）：

读作“拉姆达”，常用来表示率参数或某些正则化参数。
μ（Mu）：

读作“缪”或“谬”，常用来表示均值。
σ（Sigma）：

读作“西格玛”，常用来表示标准差或方差（平方后）。
φ（Phi）：

读作“费”或“菲”，常用来表示某些角度或分布参数。
ρ（Rho）：

读作“柔”或“罗”，常用来表示相关系数。
